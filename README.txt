The repository consists of StringAccululator and corresponding test StringAccululatorTest

Please download, compile and run test by running below commands:

$ git clone https://github.com/mbans/string-accumulator.git
$ mvn clean test

Alternatively, download and then run tests from IDE
